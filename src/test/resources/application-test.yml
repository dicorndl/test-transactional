spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MYSQL
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: create-drop
    open-in-view: false
  config:
    activate:
      on-profile: test